#include <integration_contract_minimal.h>

namespace chickpea {

	void init(global_struct*, char*);

	void destroy(global_struct*);


	bool shouldExit(global_struct*);

	void processInput(global_struct*);


	void engine_draw_frame(global_struct*);

}